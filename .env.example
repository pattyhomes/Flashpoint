# Copy to .env and fill in values. Never commit .env.

APP_ENV=development
APP_HOST=127.0.0.1
APP_PORT=8000
APP_DEBUG=true

DATABASE_URL=sqlite:///../data/flashpoint.db

INGESTION_INTERVAL_SECONDS=1800
MOCK_DATA_ENABLED=true

# Ingestion source: "mock" (dev/demo) or "gdelt" (production real-data)
INGEST_SOURCE=mock

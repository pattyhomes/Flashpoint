# Server-side clustering placeholder.
# For now, clustering is handled on the frontend (e.g. Supercluster / MapLibre).
# This module is reserved for future server-side pre-clustering if needed.


def cluster_events(events: list, zoom: int = 10) -> list[dict]:
    """Return clustered event groups for a given zoom level."""
    return []

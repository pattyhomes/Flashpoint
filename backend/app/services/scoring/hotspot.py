# Hotspot scoring: identifies geographic areas with elevated event density or severity.
# TODO: implement grid-based or KDE scoring once event volume justifies it.


def score_hotspots(events: list) -> list[dict]:
    """Return a list of hotspot regions, each with a score and center coordinates."""
    return []
